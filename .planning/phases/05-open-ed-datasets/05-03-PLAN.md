---
phase: 05-open-ed-datasets
plan: 03
type: execute
---

<objective>
Ingest the verbal / logical-reasoning datasets approved in LICENSES.md and
normalize them into the same row schema as the math corpus.

Purpose: CAT verbal ability + LR/DI sections and most reasoning-heavy exam
content need this. Without it the tutor will only be able to answer math.

Output:
  - scripts/ingest_reasoning.py
  - datasets/reasoning-corpus.jsonl    (≥ 150K rows expected)
  - datasets/reasoning-corpus.stats.json
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/05-open-ed-datasets/05-01-SUMMARY.md
@.planning/phases/05-open-ed-datasets/05-02-SUMMARY.md
@.planning/phases/05-open-ed-datasets/LICENSES.md
@scripts/ingest_math.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scripts/ingest_reasoning.py</name>
  <files>scripts/ingest_reasoning.py</files>
  <action>
    Mirror the structure of scripts/ingest_math.py (same CLI flags, same
    streaming approach, same output schema), but with extractors for the
    INCLUDE=yes reasoning/verbal datasets from LICENSES.md. Minimum
    supported list:

      - cais/mmlu               → 57 subjects, MCQ.
          problem = question + "\nOptions: " + " | ".join(choices)
          answer  = chr(65 + answer_index)  # 'A'/'B'/'C'/'D'
          solution = null
          subDomain heuristic = mmlu_subject_to_subdomain(subject)
            (humanities → verbal, formal_logic → logic, stem-other → reasoning)
      - allenai/ai2_arc (both challenge + easy)
          problem = question + options serialized as above
          answer  = answerKey
          subDomain = "science-reasoning"
      - google/boolq
          problem = passage + "\nQuestion: " + question
          answer  = "yes" if answer else "no"
          subDomain = "reading-comprehension"
      - lukaemon/bbh             → 27 BIG-Bench Hard tasks
          problem = input
          answer  = target
          subDomain = "logical-reasoning"
      - EleutherAI/lambada_openai (only if license allows; if not, skip)
          problem = text  (cloze)
          answer  = last word
          subDomain = "language-modeling"

    Use the same `subject` field set to "reasoning" (top-level). Carry
    license/url through from LICENSES.md. Same skip rules (problem ≥ 20
    chars, answer non-empty).

    Avoid: do NOT pull anything not on the LICENSES.md INCLUDE=yes list.
    If a candidate is missing from LICENSES.md, fail loudly in the script
    rather than silently including it.
  </action>
  <verify>
    source ~/autogluon-env/bin/activate
    python scripts/ingest_reasoning.py --limit-per-source 50
    wc -l datasets/reasoning-corpus.jsonl   # ≥ 150
    python -c "
import json
rows = [json.loads(l) for l in open('datasets/reasoning-corpus.jsonl')]
sources = {r['source'] for r in rows}
assert len(sources) >= 3, sources
assert all(r['license'] for r in rows), 'unlicensed row found'
print('OK', len(rows), sources)
"
  </verify>
  <done>
    Script produces a valid JSONL with ≥ 3 sources represented, no
    unlicensed rows, schema matches math-corpus.jsonl.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full ingestion run + sanity report</name>
  <files>datasets/reasoning-corpus.jsonl, datasets/reasoning-corpus.stats.json</files>
  <action>
    Run `python scripts/ingest_reasoning.py` with default cap. Background
    via `run_in_background`. After it finishes:

    1. cat datasets/reasoning-corpus.stats.json
    2. Inline python report: total rows, per-source rows, 3 random sample
       rows printed.
    3. Per-subDomain row counts (group by `subDomain` field).
    4. Confirm ≥ 150K rows and ≥ 4 distinct subDomains.

    Skip a source rather than crashing if it disappeared on HF.
  </action>
  <verify>
    test $(wc -l < datasets/reasoning-corpus.jsonl) -ge 150000
    python -c "
import json, collections
rows = (json.loads(l) for l in open('datasets/reasoning-corpus.jsonl'))
c = collections.Counter(r['subDomain'] for r in rows)
print(c)
assert len(c) >= 4, c
"
  </verify>
  <done>
    ≥ 150K rows, ≥ 4 subDomains represented, stats.json matches the JSONL.
  </done>
</task>

</tasks>

<verification>
- [ ] datasets/reasoning-corpus.jsonl exists and has ≥ 150K rows
- [ ] Schema matches the math corpus exactly (same field names + types)
- [ ] No row has license = "" or license = "unknown"
- [ ] ≥ 4 distinct subDomain values present
- [ ] stats.json row sum equals the JSONL line count
</verification>

<success_criteria>
- Reasoning sub-corpus exists alongside math sub-corpus
- Both share an identical schema → trivial concat in Phase 7
- Verbal + logic coverage is real, not just one or two datasets
</success_criteria>

<output>
After completion, create `.planning/phases/05-open-ed-datasets/05-03-SUMMARY.md`:

# Phase 5 Plan 03: Reasoning + verbal corpus ingestion — Summary

**Ingested N rows of reasoning/verbal Q&A across M datasets and K subDomains.**

## Accomplishments
- (per-source row counts)
- (per-subDomain row counts)

## Files Created/Modified
- `scripts/ingest_reasoning.py`
- `datasets/reasoning-corpus.jsonl`
- `datasets/reasoning-corpus.stats.json`

## Decisions Made
- (mapping rules for MMLU subjects → subDomain)
- (any sources skipped + why)

## Issues Encountered
- (rate limits, schema surprises)

## Next Step
Ready for 05-04-PLAN.md (CC-licensed long-form textbook content).
</output>
